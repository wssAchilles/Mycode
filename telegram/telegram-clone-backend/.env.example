# =================================
# 后端环境变量模板 (生产环境)
# 复制此文件为 .env 并填写实际值
# =================================

# 服务器配置
NODE_ENV=production
PORT=10000

# MongoDB Atlas (已有)
# 格式: mongodb+srv://user:password@cluster.mongodb.net/database
MONGODB_URI=mongodb+srv://your_user:your_password@your_cluster.mongodb.net/telegram_clone?retryWrites=true&w=majority

# PostgreSQL (Supabase)
# 从 Supabase Dashboard -> Settings -> Database -> Connection string 获取
DATABASE_URL=postgresql://postgres:your_password@db.your_project.supabase.co:5432/postgres

# Redis (Upstash)
# 从 Upstash Console -> Redis Database -> Details 获取
REDIS_URL=rediss://default:your_password@your_endpoint.upstash.io:6379

# JWT 密钥 (生产环境请使用强密钥)
# 生成命令: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_at_least_64_characters_long
JWT_EXPIRES_IN=7d

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key

# AI Socket (生产环境建议禁用)
AI_SOCKET_ENABLED=false
