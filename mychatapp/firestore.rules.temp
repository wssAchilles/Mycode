rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // 临时开放规则 - 仅用于调试
    // 任何已认证用户都可以读写所有文档
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
  }
}
