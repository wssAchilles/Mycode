# 🚀 VitePress + GitHub Pages 部署完整指南

本指南将详细说明如何将文档部署到 GitHub Pages,实现自动化发布。

## 📋 前置条件检查

在开始之前,确保你已经:

- ✅ 安装了 Node.js 和 npm
- ✅ 项目已推送到 GitHub
- ✅ 有 GitHub 账号的写权限

## 🎯 第一步:准备文档内容

### 1.1 本地预览

在 `docs` 目录下运行:

```powershell
cd docs
npm install
npm run docs:dev
```

访问 http://localhost:5173/Mycode/ 查看效果。

### 1.2 检查配置

确认 `docs/.vitepress/config.js` 中的 `base` 配置正确:

```javascript
export default {
  base: '/Mycode/', // 必须与 GitHub 仓库名一致
  // ... 其他配置
}
```

::: warning 注意
`base` 必须与你的 GitHub 仓库名完全一致,否则部署后样式和链接会失效!
:::

## 🔧 第二步:配置 GitHub Actions

### 2.1 确认 Workflow 文件

确保文件 `.github/workflows/deploy-docs.yml` 存在。

这个文件会在你推送代码到 `main` 分支且修改了 `docs/` 目录时自动触发部署。

### 2.2 检查文件内容

```yaml
name: Deploy Documentation

on:
  push:
    branches:
      - main  # 确认分支名称正确
    paths:
      - 'docs/**'
      - '.github/workflows/deploy-docs.yml'
  workflow_dispatch:  # 允许手动触发

# ... 其他配置
```

## 📤 第三步:推送代码到 GitHub

### 3.1 提交所有更改

在项目根目录运行:

```powershell
# 查看修改的文件
git status

# 添加所有文件
git add .

# 提交更改
git commit -m "docs: add VitePress documentation site"

# 推送到 GitHub
git push origin main
```

::: tip 提示
如果你的默认分支是 `master` 而不是 `main`,记得修改 workflow 文件和推送命令。
:::

### 3.2 验证推送成功

1. 访问你的 GitHub 仓库
2. 检查文件是否已更新
3. 确认 `docs/` 目录和 `.github/workflows/` 都已推送

## ⚙️ 第四步:配置 GitHub Pages

### 4.1 进入仓库设置

1. 打开你的 GitHub 仓库: `https://github.com/wssAchilles/Mycode`
2. 点击 **Settings** (设置)
3. 在左侧菜单找到 **Pages**

### 4.2 配置 Pages 源

在 **Source** 部分:

1. 选择 **GitHub Actions** (而不是 Deploy from a branch)
2. 保存设置

![GitHub Pages 设置](https://docs.github.com/assets/cb-47267/images/help/pages/creating-publishing-source.png)

::: info 说明
选择 GitHub Actions 后,系统会自动使用你的 workflow 文件进行部署。
:::

### 4.3 配置 Actions 权限

1. 在仓库 Settings 中找到 **Actions** → **General**
2. 滚动到 **Workflow permissions**
3. 选择 **Read and write permissions**
4. 勾选 **Allow GitHub Actions to create and approve pull requests**
5. 点击 **Save** 保存

![Workflow 权限设置](https://docs.github.com/assets/cb-25233/images/help/settings/actions-workflow-permissions.png)

## 🚀 第五步:触发部署

### 5.1 自动触发

推送代码后,GitHub Actions 会自动运行。你可以:

1. 进入仓库的 **Actions** 标签页
2. 查看 "Deploy Documentation" workflow
3. 观察构建进度

### 5.2 手动触发(可选)

如果需要手动触发部署:

1. 进入 **Actions** 标签页
2. 选择 **Deploy Documentation** workflow
3. 点击 **Run workflow** 按钮
4. 选择 `main` 分支
5. 点击 **Run workflow** 确认

## ✅ 第六步:验证部署

### 6.1 检查 Actions 状态

1. 在 **Actions** 标签页,等待 workflow 运行完成(通常需要 1-3 分钟)
2. 确认显示绿色的 ✓ 表示成功
3. 如果显示红色 ✗,点击查看日志排查问题

### 6.2 访问文档站点

部署成功后,访问:

```
https://wssAchilles.github.io/Mycode/
```

::: tip 提示
首次部署可能需要等待 5-10 分钟才能访问,请耐心等待。
:::

### 6.3 验证功能

检查以下功能是否正常:

- ✅ 页面正常显示
- ✅ 导航栏链接可用
- ✅ 侧边栏展开正常
- ✅ 搜索功能工作
- ✅ 代码高亮显示
- ✅ 暗黑模式切换

## 🔄 日常更新流程

### 更新文档内容

1. **编辑文档**
   ```powershell
   # 编辑 docs/ 目录下的 Markdown 文件
   code docs/guide/getting-started.md
   ```

2. **本地预览**
   ```powershell
   cd docs
   npm run docs:dev
   ```

3. **提交并推送**
   ```powershell
   git add docs/
   git commit -m "docs: update getting started guide"
   git push origin main
   ```

4. **自动部署**
   - GitHub Actions 自动检测到变更
   - 自动构建并部署
   - 1-3 分钟后生效

## 🐛 常见问题排查

### 问题 1: 404 页面

**症状**: 访问文档站点显示 404

**原因**: `base` 配置不正确

**解决**:

1. 检查 `docs/.vitepress/config.js` 中的 `base` 设置
2. 确保 `base: '/Mycode/'` 与仓库名完全一致
3. 注意大小写和斜杠

### 问题 2: 样式丢失

**症状**: 页面显示但没有样式

**原因**: 静态资源路径错误,也是 `base` 配置问题

**解决**: 同问题 1

### 问题 3: Actions 失败

**症状**: GitHub Actions 显示红色 ✗

**排查步骤**:

1. **点击失败的 workflow** 查看详细日志

2. **常见错误及解决方案**:

   **错误**: `Permission denied`
   ```
   解决: 检查 Actions 权限设置,需要 Read and write
   ```

   **错误**: `npm install failed`
   ```
   解决: 检查 package.json 依赖是否正确
   ```

   **错误**: `Build failed`
   ```
   解决: 本地运行 npm run docs:build 检查错误
   ```

### 问题 4: 部署成功但无法访问

**症状**: Actions 显示成功,但访问 URL 无响应

**解决**:

1. **确认 Pages 已启用**
   - Settings → Pages
   - 确认显示 "Your site is live at ..."

2. **检查分支**
   - 确认部署到正确的分支
   - 默认应该是 `gh-pages` 分支

3. **等待 DNS 生效**
   - 首次部署可能需要 10-15 分钟
   - 刷新缓存: Ctrl + F5

### 问题 5: 搜索功能不工作

**症状**: 搜索框无反应或搜索不到内容

**原因**: 构建时未生成搜索索引

**解决**:

1. 确认配置文件中有搜索配置:
   ```javascript
   search: {
     provider: 'local'
   }
   ```

2. 重新构建部署
3. 清除浏览器缓存

## 📊 部署状态监控

### 查看部署历史

1. 进入 **Actions** 标签页
2. 查看所有运行记录
3. 点击任意记录查看详细日志

### 设置通知

1. 进入 **Settings** → **Notifications**
2. 配置 Actions 失败时的邮件通知
3. 及时发现部署问题

## 🎨 自定义域名(可选)

### 使用自定义域名

如果你有自己的域名:

1. **添加 CNAME 文件**
   ```powershell
   echo "docs.yourdomain.com" > docs/public/CNAME
   ```

2. **配置 DNS**
   - 添加 CNAME 记录
   - 指向: `wssAchilles.github.io`

3. **在 GitHub 设置**
   - Settings → Pages
   - Custom domain: 输入你的域名
   - Save

4. **等待 SSL 证书生效**
   - 勾选 "Enforce HTTPS"
   - 等待几分钟

## 🚀 性能优化

### 1. 启用 CDN

GitHub Pages 自带 CDN,自动优化。

### 2. 图片优化

- 使用 WebP 格式
- 压缩图片大小
- 使用外部图床(如 GitHub Issues)

### 3. 减少构建时间

```javascript
// config.js
export default {
  // 只在生产环境生成 sitemap
  sitemap: {
    hostname: 'https://wssAchilles.github.io/Mycode/'
  }
}
```

## 📈 SEO 优化

### 1. 添加 sitemap

VitePress 自动生成,确保配置:

```javascript
export default {
  sitemap: {
    hostname: 'https://wssAchilles.github.io/Mycode/'
  }
}
```

### 2. 优化 meta 标签

已在配置文件中设置:

```javascript
head: [
  ['meta', { name: 'description', content: '计算机408可视化学习平台' }],
  ['meta', { name: 'keywords', content: 'Flutter, 408考研, 算法可视化' }]
]
```

### 3. 提交到搜索引擎

- Google Search Console
- Bing Webmaster Tools

## 🎯 下一步

现在你的文档站点已经成功部署!你可以:

- ✅ 继续完善文档内容
- ✅ 添加更多页面和章节
- ✅ 自定义主题样式
- ✅ 配置多语言支持
- ✅ 集成评论系统

## 📞 需要帮助?

如果遇到问题:

1. **查看官方文档**
   - [VitePress 文档](https://vitepress.dev/)
   - [GitHub Pages 文档](https://docs.github.com/pages)

2. **搜索已有问题**
   - [VitePress Issues](https://github.com/vuejs/vitepress/issues)
   - [GitHub Community](https://github.community/)

3. **提交 Issue**
   - 描述详细的错误信息
   - 附上相关日志和截图

---

## ✨ 总结

恭喜!你已经完成了 VitePress 文档站点的部署。

**你获得了**:
- 🎉 免费的在线文档托管
- 🚀 自动化的部署流程  
- 🔍 强大的全文搜索功能
- 📱 响应式的现代化界面
- 🌙 暗黑模式支持

**记住**:
- 每次推送 `docs/` 目录的修改都会自动部署
- 部署通常需要 1-3 分钟生效
- 保持文档与代码同步更新

**开始写作吧!** 📝

访问你的文档: [https://wssAchilles.github.io/Mycode/](https://wssAchilles.github.io/Mycode/)
