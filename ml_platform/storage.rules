rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 允许已登录用户读取和写入 datasets 目录下的文件
    match /datasets/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    // 默认禁止其他路径的访问
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
